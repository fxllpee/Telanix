

# Ambiente (development | production)
NODE_ENV=development

# Porta do servidor
PORT=3001

# Database Local (PostgreSQL)
DATABASE_URL=postgresql://postgres:senha@localhost:5432/telanix_db

# Database Produção (Render - substituir com URL do Render)
# DATABASE_URL=postgresql://usuario:senha@host.render.com:5432/database

# Frontend URLs (para CORS)
FRONTEND_URL_DEV=http://localhost:8080
FRONTEND_URL_PROD=https://telanix.onrender.com

# JWT Secret (gerar um aleatório em produção)
JWT_SECRET=meu_segredo_super_secreto_mudar_em_producao

